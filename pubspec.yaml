name: flutter_othello
description: Flutter Othello project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: any

flutter_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/icons/app_icon.png"

flutter:
  uses-material-design: true
